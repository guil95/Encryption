# Encryption

# Encrypt
```php
/**
 * @param string $value
 * @return string
 */
$encrypt = (new Encryption())->encrypt($value);
```
# Decrypt
```php
/**
 * @param string $encrypt
 * @return string
 */
$decrypt = (new Encryption())->decrypt($encrypt);
```
